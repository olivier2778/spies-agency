{% extends 'base.html.twig' %}

{% block title %}Modifier une Mission{% endblock %}

{% block main %}

    <div class="row">

        <div class="col-4"></div>   

        <div class="col-4">             
            {{ include('mission/_form.html.twig', {'button_label': 'Modifier'}) }}
            <a href="{{ path('mission_index') }}" class ="link link-primary text-end">Retour à la liste</a>
            {{ include('mission/_delete_form.html.twig') }}            
        </div>   

    </div>   
    
{% endblock %}
