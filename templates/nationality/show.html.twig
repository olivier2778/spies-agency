{% extends 'base.html.twig' %}

{% block title %}Nationality{% endblock %}

{% block body %}
    <h1>Nationalité</h1>

    <table class="table">
        <tbody>            
            <tr>
                <th>Nationalité</th>
                <td>{{ nationality.nationalityName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nationality_index') }}">Retour à la liste</a>

    <a href="{{ path('nationality_edit', {'id': nationality.id}) }}">Modifier</a>

    {{ include('nationality/_delete_form.html.twig') }}
{% endblock %}
