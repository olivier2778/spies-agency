{% extends 'base.html.twig' %}

{% block title %}Planque{% endblock %}

{% block main %}
  
    <table class="table">
        <tbody>           
            <tr>
                <th>Code</th>
                <td>{{ hideaway.hideawayCode }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ hideaway.hideawayAdress }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hideaway_index') }}">Retour à la liste</a>

    <a href="{{ path('hideaway_edit', {'id': hideaway.id}) }}">Modifier</a>

    {{ include('hideaway/_delete_form.html.twig') }}
{% endblock %}
